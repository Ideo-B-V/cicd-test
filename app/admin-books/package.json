{
  "name": "genres",
  "version": "1.0.0",
  "main": "webapp/index.html",
  "scripts": {
    "build": "ui5 build preload --clean-dest",
    "start": "ui5 serve",
    "ci-lint": "echo \"No ESLint configured for this UI module\"",
    "ci-audit": "npm audit --omit=dev --audit-level=high"
  },
  "devDependencies": {
    "@ui5/cli": "^4",
    "ui5-task-zipper": "^3",
    "ui5-tooling-transpile": "^3.12.0"
  }
}
